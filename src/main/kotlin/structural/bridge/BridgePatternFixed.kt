package org.example.structural.bridge

/**
 * The idea behind the Bridge design pattern is to flatten the class hierarchy and have fewer specialized classes in our system.
 * It also helps us to avoid the fragile base class problem when modifying the superclass introduces subtle bugs to classes that inherit from it.
 */
class BridgePatternFixed
